<div class="flex justify-center items-center w-full">
<button
  class="w-64 h-16  /* fast bredde og højde */
         inline-flex items-center justify-center
         bg-transparent
         border border-[var(--color-b-txt)] text-[var(--color-b-txt)]
         transition-colors duration-200
         hover:bg-black/60 hover:border-black hover:text-white">
  <span class="font-subheading leading-none text-[length:var(--h2-size)]">
    <slot />
  </span>
</button>

</div>
